import 'package:auto_matic/config/config.dart';

class VehiculeCard extends StatelessWidget {
  const VehiculeCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
